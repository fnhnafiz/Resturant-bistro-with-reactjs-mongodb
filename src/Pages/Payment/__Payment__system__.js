/**
 * first step to payment system : instal stripe in npm
 *
 * second step : create a varriable outof the components and import (loadStripe()) and in the loadStripe using string and declare a varribale PK(Publicasion key)
 *
 * thir step : create a elements in stripe <Elements> </Elements>
 * Fourth step : in the Elements element send to Props stripe={publicasion key variable}
 *
 */
